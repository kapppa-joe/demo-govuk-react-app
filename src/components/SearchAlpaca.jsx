import { SearchBox } from "govuk-react";
const SearchAlpaca = () => {
    return (
 
      <SearchBox>
        <SearchBox.Input placeholder="Search for an alpaca" />
        <SearchBox.Button />
      </SearchBox>
    );
};

export default SearchAlpaca;